const styles = theme => ({
  '@global html, body, #root': {
    margin: 0,
    boxSizing: 'border-box',
    height: '100%',
    fontFamily: '"Open Sans", sans-serif',
  }
});

export default styles;